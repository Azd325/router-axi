#!/usr/bin/env sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$root"
binary="$root/bin/.router-axi"
nix develop --no-pure-eval --command go build -o "$binary" ./cmd/router-axi
exec "$binary" "$@"
